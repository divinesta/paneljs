# Fastify

Not published. There is no `@paneljs/fastify` package yet.

Express is the HTTP adapter that ships: [`mount(app, admin)`](/frameworks/express).

When Fastify lands, install will live at [Installation → Fastify](/guide/installation/fastify). `createAdmin` and `register` will not change.
