# Nest.js

Not published. There is no `@paneljs/nest` package yet.

Express is the HTTP adapter that ships: [`mount(app, admin)`](/frameworks/express).

When Nest lands, install will live at [Installation → Nest.js](/guide/installation/nestjs). `createAdmin` and `register` will not change.
